Overview

Design Space Exploration

Different steps:
- Load models
- Analyse a given model
-- Extension Point: Solver (SimuCom, LQNSim, ...)
-- Extension Point for different quality attributes (Meta model!)
-- TODO: Define common result meta model
- Generate new candidates
-- Extension Point: For which quality attribute (Meta model!)
-- Extension Point: Allocation, Resource Env. Tuning, Component Selection
-- Make these configurable. Show available extensions, group by quality attribute and let user select. 
-- Also prioritise which one is used more likely than another (configuration file that is changeable in Eclipse preferences)    
   